name: Lockfile generator disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The production lockfile is committed. This one-shot workflow is disabled."
